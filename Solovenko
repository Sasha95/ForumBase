SET ROLE user_mangir;
SET "jwt.user_id" TO 2;
DROP POLICY tag_post_update ON forum.post;
CREATE POLICY tag_post_update ON forum.post FOR UPDATE TO user_mangir
USING (current_setting('jwt.user_id')::int IN (SELECT person_id FROM post WHERE post_id=id));


GRANT ALL ON forum.tag_post TO user_mangir;
